name: encriptacio_publicacio
description: Aplicació Flutter per encriptar i desencriptar arxius amb RSA.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^8.1.7
  pointycastle: ^3.9.1
  basic_utils: ^5.7.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true